# CaseMap core pipeline dependencies.
#
# Provenance matters here — see HANDOFF.md §2 for the full picture. Two different
# levels of confidence in this file:
#
#   VERIFIED THIS SESSION (2026-09-09): the spacy/pydantic/numpy/thinc chain below is
#   pinned to the exact combination that was actually installed and confirmed working
#   for en_legal_ner_sm on this machine (Windows, Python 3.11) — see
#   docs/research/new-directions/mini-llm-extraction-assessment.md Part 2 for the full
#   story of what breaks without these exact pins (two pre-existing conflicting spaCy
#   installs, a pydantic v1/v2 incompatibility in spaCy 3.2.x's schemas on Python 3.11,
#   an invalid wheel filename HuggingFace serves for en_legal_ner_sm). Do not loosen
#   these without re-reading that section first.
#
#   CARRIED FROM THE PRE-GOVERNANCE HANDOFF, NOT RE-VERIFIED THIS SESSION: PyMuPDF,
#   OpenCV, rapidfuzz, dateparser, sentence-transformers, PaddleOCR/Surya/Tesseract —
#   these are documented as working in CaseMap_AGENT_HANDOFF.md §8 from an earlier
#   session, but were not reinstalled/reconfirmed in this session's work (which used
#   standalone venvs for POC work, not this file). Treat versions below as reasonable
#   starting points, not verified pins, until someone actually runs `pip install -r
#   requirements.txt` end to end and updates this comment.

# --- PDF / OCR ingestion (stage 0) ---
pymupdf                    # PyMuPDF — not re-verified this session, see note above
opencv-python-headless     # not re-verified this session
pillow
pytesseract                 # needs system tesseract-ocr binary installed separately
# paddleocr / surya-ocr    # NOT installed/confirmed in any session to date — see
                             # CaseMap_AGENT_HANDOFF.md §8. Uncomment and test before
                             # relying on either as the OCR tier.

# --- deterministic extraction (stage 3) ---
rapidfuzz                   # not re-verified this session — used by is_versus_line(),
                             # gracefully degrades to exact-match-only if missing
dateparser                  # not re-verified this session

# --- semantic similarity (stage 5, make_similarity_fn) ---
sentence-transformers       # MiniLM default (EMBED_MODEL in casemap_service.py).
                             # Qwen3-Embedding-0.6B was evaluated as an upgrade
                             # (FINDINGS.md F-10/F-15) and rejected 2026-09-11:
                             # better separation on a small sample, but ~112x
                             # slower on CPU — stays on MiniLM.

# --- mandatory ML layer (stage 2, opennyai_bridge.py) — VERIFIED 2026-09-09 ---
spacy==3.8.16
pydantic>=2.0.0,<3.0.0
numpy>=2.0
thinc==8.3.13
typer==0.27.2
weasel==1.0.0
wasabi==1.1.3
# en_legal_ner_sm itself is NOT on PyPI and CANNOT go in this file — the wheel
# HuggingFace serves has an invalid PEP 440 version segment ("any") that pip rejects
# outright, even as a direct URL install (confirmed 2026-09-09: `pip install
# https://huggingface.co/opennyaiorg/en_legal_ner_sm/resolve/main/en_legal_ner_sm-any-py3-none-any.whl`
# fails with "Invalid wheel filename (invalid version)"). Install it manually:
#
#   curl -sL -o en_legal_ner_sm-3.2.0-py3-none-any.whl \
#     https://huggingface.co/opennyaiorg/en_legal_ner_sm/resolve/main/en_legal_ner_sm-any-py3-none-any.whl
#   pip install en_legal_ner_sm-3.2.0-py3-none-any.whl
#
# It will pull in its own old spacy<3.3.0/pydantic<1.9.0 pins as transitive deps —
# reinstall spacy/pydantic/thinc/typer/weasel/wasabi at the versions pinned above
# AFTER installing the wheel, or you will hit the exact Python 3.11 / pydantic v1
# incompatibility this session spent real time debugging (see the comment at the top
# of this file). Full recipe: docs/research/new-directions/mini-llm-extraction-assessment.md
# Part 2.
