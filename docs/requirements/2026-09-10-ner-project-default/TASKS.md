# Tasks

## T1 — Create `.venv` and install NER via the existing script

**Acceptance:** FR2 command succeeds in `.venv`.

## T2 — Document the default interpreter

README (or STATUS/HANDOFF) states: project default is `.venv`, not system
Python and not only `temp/.../venv_ner`.
