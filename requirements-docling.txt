# OPTIONAL, heavy (~1.5GB with models). Only needed for
# docs/requirements/2026-09-09-structure-and-embedding-layers/ implementation work
# (layout_structure.py's Docling-backed detect_structure_layered()). Not needed to run
# the existing pipeline — install requirements.txt alone for that.
#
# Verified working together 2026-09-09 (see docs/research/new-directions/
# layer-additions-assessment.md): this was installed in its OWN standalone venv,
# never combined in the same environment as requirements.txt's spacy==3.8.16/
# en_legal_ner_sm chain. If installing both in one environment for real T2/T4
# implementation work (docs/requirements/2026-09-09-structure-and-embedding-layers/
# TASKS.md), re-verify there's no dependency conflict before assuming this file and
# requirements.txt combine cleanly — they were never tested together.

docling==2.126.0

# NOT included here, and NOT yet verified (TASKS.md T3): whether
# sentence-transformers can load "Qwen/Qwen3-Embedding-0.6B" directly. The POC
# (docs/research/new-directions/layer-additions-assessment.md) used Ollama's local
# API instead — a completely different code path from the sentence_transformers one
# make_similarity_fn() actually uses in src/casemap_pipeline.py. Confirm T3 before
# assuming `pip install sentence-transformers` + this model name just works.
